services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: onshape
      POSTGRES_USER: onshape
      POSTGRES_PASSWORD: onshape
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
